name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  resolve-version:
    runs-on: ubuntu-latest
    outputs:
      version-name: ${{ steps.version.outputs.version-name }}
      tag: ${{ steps.version.outputs.tag }}
      release-name: ${{ steps.version.outputs.release-name }}
      artifact-prefix: ${{ steps.version.outputs.artifact-prefix }}
    steps:
      - uses: actions/checkout@v4
      - name: Extract version info
        id: version
        run: |
          VERSION=$(grep '^version-name\s*=' gradle/libs.versions.toml | sed 's/.*"\(.*\)".*/\1/')
          echo "version-name=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=compose-animation-$VERSION" >> "$GITHUB_OUTPUT"
          echo "release-name=Compose Animation $VERSION" >> "$GITHUB_OUTPUT"
          echo "artifact-prefix=compose-animation-$VERSION" >> "$GITHUB_OUTPUT"

  build-android:
    needs: resolve-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Decode keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > composeApp/keystore.jks
      - name: Build release APK
        run: ./gradlew :composeApp:assembleRelease
        env:
          GRADLE_OPTS: "-Xmx6g"
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
      - name: Rename artifact
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cp composeApp/build/outputs/apk/release/composeApp-release.apk "$PREFIX.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}.apk

  build-desktop-linux:
    needs: resolve-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Build deb package
        run: ./gradlew :composeApp:packageDeb
        env:
          GRADLE_OPTS: "-Xmx6g"
      - name: Rename artifact
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cp composeApp/build/compose/binaries/main/deb/*.deb "$PREFIX.deb"
      - uses: actions/upload-artifact@v4
        with:
          name: linux-deb
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}.deb

  build-desktop-linux-rpm:
    needs: resolve-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Build rpm package
        run: ./gradlew :composeApp:packageRpm
        env:
          GRADLE_OPTS: "-Xmx6g"
      - name: Rename artifact
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cp composeApp/build/compose/binaries/main/rpm/*.rpm "$PREFIX.rpm"
      - uses: actions/upload-artifact@v4
        with:
          name: linux-rpm
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}.rpm

  build-desktop-linux-appimage:
    needs: resolve-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Build AppImage
        run: ./gradlew :composeApp:packageAppImage
        env:
          GRADLE_OPTS: "-Xmx6g"
      - name: Rename artifact
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cp composeApp/build/compose/binaries/main/app-image/*.AppImage "$PREFIX.AppImage"
      - uses: actions/upload-artifact@v4
        with:
          name: linux-appimage
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}.AppImage

  build-desktop-windows:
    needs: resolve-version
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Build msi package
        run: ./gradlew :composeApp:packageMsi
        env:
          GRADLE_OPTS: "-Xmx6g"
      - name: Rename artifact
        shell: bash
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cp composeApp/build/compose/binaries/main/msi/*.msi "$PREFIX.msi"
      - uses: actions/upload-artifact@v4
        with:
          name: windows-msi
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}.msi

  build-desktop-macos:
    needs: resolve-version
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Build dmg package
        run: ./gradlew :composeApp:packageDmg
        env:
          GRADLE_OPTS: "-Xmx6g"
      - name: Rename artifact
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cp composeApp/build/compose/binaries/main/dmg/*.dmg "$PREFIX.dmg"
      - uses: actions/upload-artifact@v4
        with:
          name: macos-dmg
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}.dmg

  build-web-wasm:
    needs: resolve-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Build WASM distribution
        run: ./gradlew :composeApp:wasmJsBrowserDistribution
        env:
          GRADLE_OPTS: "-Xmx6g"
      - name: Zip output
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cd composeApp/build/dist/wasmJs/productionExecutable
          zip -r "$GITHUB_WORKSPACE/$PREFIX-wasm.zip" .
      - uses: actions/upload-artifact@v4
        with:
          name: web-wasm
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}-wasm.zip

  build-web-js:
    needs: resolve-version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Build JS distribution
        run: ./gradlew :composeApp:jsBrowserDistribution
        env:
          GRADLE_OPTS: "-Xmx6g"
      - name: Zip output
        run: |
          PREFIX="${{ needs.resolve-version.outputs.artifact-prefix }}"
          cd composeApp/build/dist/js/productionExecutable
          zip -r "$GITHUB_WORKSPACE/$PREFIX-js.zip" .
      - uses: actions/upload-artifact@v4
        with:
          name: web-js
          path: ${{ needs.resolve-version.outputs.artifact-prefix }}-js.zip

  create-release:
    needs:
      - resolve-version
#      - build-android
      - build-desktop-linux
      - build-desktop-linux-rpm
      - build-desktop-linux-appimage
      - build-desktop-windows
      - build-desktop-macos
      - build-web-wasm
      - build-web-js
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ needs.resolve-version.outputs.tag }}"
          RELEASE_NAME="${{ needs.resolve-version.outputs.release-name }}"

          gh release create "$TAG" \
            --title "$RELEASE_NAME" \
            --generate-notes \
            artifacts/*